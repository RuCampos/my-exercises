public class Player2 {
    public static int Move(int numbers){

       // int choice = 0;

        //while player one starts at 1 and adds 1 player 2 starts at the end and subtracts

       // for(int i = numbers; i > 0 ; i--){

           // choice = i + 1; //we dont need to enumerate all the options, we can go one by one until we have said all numbers

           // return choice; //is returning 1 only and isnt allowing to go to the other return
        //}

        return NumberGenerator.chooseNumber(numbers);
        //return choice; is returning 10 only

       // return choice = 0;
    };

}
